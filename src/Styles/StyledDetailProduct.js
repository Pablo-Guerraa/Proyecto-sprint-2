import styled from "styled-components";

export const StyledContDet = styled.div`
  margin: 0 0 32px 0;
  text-align: ${(props)=> props.textAlign && 'center'};
`;
export const StyledPrice = styled.p`
  margin-top: 8px;
  color: #FA4A0C;
`;